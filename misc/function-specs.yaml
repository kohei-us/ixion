---
# Specifications of built-in functions alreay implemented in ixion.
# Keep them sorted alphabetically.
average:
  category: statistical
  params:
    min: 1
    max: null
concatenate:
  category: text
  params:
    min: null
    max: null
counta:
  category: statistical
  params:
    min: null
    max: null
if:
  category: logical
  params:
    min: 3
    max: 3
int:
  category: mathematical
  params:
    min: 1
    max: 1
left:
  category: text
  params:
    min: 1
    max: 2
len:
  category: text
  params:
    min: 1
    max: 1
max:
  category: statistical
  params:
    min: 1
    max: null
min:
  category: statistical
  params:
    min: 1
    max: null
mmult:
  category: matrix
  params:
    min: 2
    max: 2
now:
  category: time
  params:
    min: 0
    max: 0
  note:
    This function currently does not account for the zero date.
pi:
  category: mathematical
  params:
    min: 0
    max: 0
subtotal:
  category: mathematical
  params:
    min: 2
    max: 2
  note:
    This function is not fully implemented.
sum:
  category: mathematical
  params:
    min: 1
    max: null
wait:
  category: development
  params:
    min: 0
    max: 0
  note:
    This function is used for development only.  It sleeps for 1 second and
    always returns 1.
